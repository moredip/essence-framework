{
  "name": "essence-framework",
  "version": "0.0.2",
  "description": "An http framework that removes all accidental complexity",
  "bin": {
    "essence": "dist/cli/essence.js"
  },
  "scripts": {
    "test": "jest",
    "build": "rm -rf dist/* && tsc",
    "prepublishOnly": "$npm_execpath test && $npm_execpath build"
  },
  "keywords": [
    "web framework"
  ],
  "author": "Pete Hodgson",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.16",
    "@types/jest": "^29.4.0",
    "jest": "^29.4.1",
    "ts-jest": "^29.0.5"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4",
    "walkdir": "^0.4.1"
  }
}
